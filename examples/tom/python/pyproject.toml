[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "muxly-hello-tom-python"
version = "0.1.0"
description = "Hello TOM ctypes example for libmuxly"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["basic_client"]
